import { ReflectClassesDirective } from './reflect-classes.directive';

describe('ReflectClassesDirective', () => {
  it('should create an instance', () => {
    const directive = new ReflectClassesDirective();
    expect(directive).toBeTruthy();
  });
});
